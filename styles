.gallery-item {
    /* Set a position context for the image */
    position: relative; 
    /* Remove the fixed height/max-width and use aspect ratio trick */
    width: 100%;
    overflow: hidden; 
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    border-radius: 8px;
    
    /* 🌟 2:3 Portrait Aspect Ratio (STAYS ACTIVE everywhere) 🌟 */
    padding-top: 150%; 
}

/* Modify the image rule to fit the new container */
.gallery-item img {
    /* Make the image fill the fixed-ratio container by default */
    position: absolute; 
    top: 0;
    left: 0;
    width: 100%; 
    height: 100%;
    
    /* Default: Image covers the entire 2:3 container, cropping the sides */
    object-fit: cover; 
    display: block;
}

/* ---------------------------------------------------- */
/* 🛠️ FIX: Targeted Style for the Single Landscape Image 🛠️ */
/* ---------------------------------------------------- */
/* This targets the specific landscape image by its source path */
.gallery-item img[src*="guramumo_bu_joi-san_no_onee-chan_ga_hoshii/001.jpg"] {
    /* **The Key Fix:** Override 'cover' with 'contain'. 
       This forces the entire landscape image to fit inside the tall, portrait 
       container, preventing it from stretching the column width. */
    object-fit: contain; 
    
    /* Center the contained image horizontally and vertically */
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    
    /* Ensure the dimensions fill the container space for positioning */
    width: 100%;
    height: 100%;
}

/* 1. Define the main wrapper as a Grid */
.gallery-wrapper {
    display: grid;
    /* Create three equal-width columns (adjust 3 to your desired number of columns) */
    grid-template-columns: repeat(3, 1fr); 
    gap: 10px; /* Space between items */
}

/* 2. Your existing .gallery-item styles (keep these) */
.gallery-item {
    position: relative; 
    width: 100%; /* Important: This means 100% of the grid column */
    /* ... other styles ... */
    padding-top: 150%; /* Portrait aspect ratio */
}

/* 3. The fix for the landscape item's container (Keep this too) */
.gallery-item.landscape-item {
    /* Set to a wide ratio (e.g., 16:9) */
    padding-top: 56.25%; 
    /* Crucial: Ensure it only spans one column, even though it's wide */
    grid-column: span 1; 
}

/* 4. The image styles inside */
.gallery-item img {
    /* ... your positioning and object-fit: cover styles ... */
}

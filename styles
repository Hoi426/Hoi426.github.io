.gallery-item {
    /* Set a position context for the image */
    position: relative; 
    width: 100%;
    overflow: hidden; 
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    border-radius: 8px;
    
    /* 🌟 2:3 Portrait Aspect Ratio (STAYS ACTIVE in all orientations) 🌟 */
    padding-top: 150%; 
}

/* Modify the image rule to fit the new container */
.gallery-item img {
    /* Make the image fill the fixed-ratio container */
    position: absolute; 
    top: 0;
    left: 0;
    width: 100%; 
    height: 100%;
    object-fit: cover; 
    display: block;
}
/* The landscape media query has been removed.
    The 150% padding-top (Portrait 2:3) is now active 
    regardless of whether the phone is held portrait or landscape.
*/

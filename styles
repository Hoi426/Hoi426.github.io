.gallery-item {
    /* Set a position context for the image */
    position: relative; 
    /* Remove the fixed height/max-width and use aspect ratio trick */
    width: 100%;
    overflow: hidden; 
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    border-radius: 8px;
    
    /* 🌟 DEFAULT: 2:3 Portrait Aspect Ratio 🌟 */
    padding-top: 150%; 
}

/* Modify the image rule to fit the new container */
.gallery-item img {
    /* Make the image fill the fixed-ratio container by default */
    position: absolute; 
    top: 0;
    left: 0;
    width: 100%; 
    height: 100%;
    object-fit: cover; 
    display: block;
}

/* 📐 MEDIA QUERY: Override Aspect Ratio for Landscape/Tablet Views */
@media (min-width: 576px) {
    .gallery-item {
        /* Example 16:9 Landscape Aspect Ratio */
        padding-top: 56.25%; 
    }
}

/* ⬇️ NEW RULE: Custom style for a specific reduced-width image ⬇️ */
.gallery-item .reduced-width {
    /* 1. Reduce the width (e.g., 75% of the container width) */
    width: 75%; 
    
    /* 2. Set height back to auto to maintain the original image ratio */
    height: auto; 
    
    /* 3. Center the smaller image horizontally and vertically */
    left: 50%; /* Start 50% from the left */
    top: 50%; /* Start 50% from the top */
    transform: translate(-50%, -50%); /* Shift back by half its own width/height */
    
    /* Optional: Ensure the reduced image doesn't cover its container, 
       though 'cover' is often fine if the image itself is smaller than 
       the container's area, which it now is. */
    object-fit: contain; 
}
